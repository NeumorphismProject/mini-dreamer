# 音频素材生成管理系统 - 前端设计实现方案 v1.0

curl --location --request POST 'https://aiado.coze.site/run' \
  --header 'Authorization: Bearer <YOUR_TOKEN>' \
  --header 'Content-Type: application/json' \
  --data '{"action":"","params":{}}'

token:
eyJhbGciOiJSUzI1NiIsImtpZCI6IjgxYmZmYzEyLTdkOGQtNGJjMS05YWVmLTRmNmI4ODAyMDA3YyJ9..ItCPFweyGSjiJ2qUAyKPNZW6RG0zAiGU-9-Zcmf1Xod-wT-EnP1cRJmka3VaAZvus94W6SNPJrSkDmDhaCZOBcZjeNSmz3afmdmbMzhZcDRysEuP_KhSMv_VK3ks8MJL3JlrUTa1qu06meL6MLnKHI4vU7ZEpzgQ3s4N46q0cGacdlQbIdvUa8W8JiCMOILequFtolqSvRFjBGsZxxdmtkG-bv4MH0HJsADyf8ZppNkIjzRklv-vckn2p_g4zrkWi10-fHiF_mHxcjTy-9IIMkkIa2UoUSce0Z6VLuyXohhfEjdJ6Q8BL6vkK4Ti_jSWOpMVVz3eJxLqjaCCnrf2zw

## 文档信息

| 属性 | 内容 |
|------|------|
| 项目名称 | 音频素材生成管理系统 - 前端 |
| 对应后端文档版本 | v1.0 |
| 前端版本 | v1.0 |
| 创建日期 | 2024-01-XX |
| 文档类型 | 前端设计实现方案 |

---

## 第一部分：项目概述与技术选型

### 1.1 项目目标

为用户提供一个完整的音频素材生成与管理的前端应用，支持：

- 音效和背景音乐的 AI 生成
- 已生成音频的浏览与管理
- 临时音频的保存与删除
- 多维度筛选与搜索

### 1.2 技术栈选型

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| **框架** | Next.js 16 (App Router) | 支持 SSR/SSG，适合 SEO 和性能优化 |
| **UI 组件库** | shadcn/ui | 基于 Radix UI，可定制化程度高 |
| **样式方案** | Tailwind CSS 4 | 原子化 CSS，快速构建 UI |
| **状态管理** | Zustand | 轻量级状态管理，简单易用 |
| **音频播放** | Wavesurfer.js | 专业音频可视化波形播放器 |
| **表单处理** | React Hook Form + Zod | 类型安全的表单验证 |
| **HTTP 客户端** | Axios | 请求拦截、统一错误处理 |
| **图标库** | Lucide React | 与 shadcn/ui 配套 |

### 1.3 项目约束

- 所有 API 调用统一通过后端工作流入口
- 请求格式：`{ action: string, params: object }`
- 响应格式：`{ success: boolean, data?: any, error?: ErrorInfo }`

---

## 第二部分：页面结构设计

### 2.1 页面清单

| 页面路径 | 页面名称 | 功能描述 | 对应 API |
|---------|---------|---------|---------|
| `/` | 首页 | 音频生成入口，展示快速生成表单 | `generate_audio` |
| `/sound-effects` | 音效库 | 音效列表浏览与管理 | `query_sound_effects` |
| `/background-music` | 背景音乐库 | 背景音乐列表浏览与管理 | `query_background_music` |
| `/temp-audio` | 临时音频 | 未保存的临时音频管理 | `query_temp_audio` |
| `/generate` | 生成页面 | 完整的音频生成表单 | `generate_audio` |

### 2.2 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  Navigation Header (顶部导航)                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Logo │ 首页 │ 音效库 │ 背景音乐 │ 临时音频 │ [生成按钮] ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  Page Content (页面内容区)                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                                                         ││
│  │  [根据当前路由动态渲染页面组件]                          ││
│  │                                                         ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  Audio Player Bar (全局音频播放器)                          │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ [波形图] [播放/暂停] [时长] [进度条] [音量] [下载]       ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### 2.3 路由结构

```
/                           → 首页（快速生成 + 最近音频）
├── /sound-effects          → 音效库列表页
│   └── /sound-effects/[id] → 音效详情页（可选）
├── /background-music       → 背景音乐列表页
│   └── /background-music/[id] → 背景音乐详情页（可选）
├── /temp-audio             → 临时音频管理页
├── /generate               → 完整生成表单页
│   ├── ?type=sound_effect  → 快速跳转音效生成
│   └── ?type=background_music → 快速跳转背景音乐生成
└── /api/proxy              → API 代理（避免跨域）
```

---

## 第三部分：组件设计

### 3.1 组件清单

#### 3.1.1 布局组件

| 组件名 | 路径 | 功能描述 |
|--------|------|---------|
| `AppLayout` | `components/layout/app-layout.tsx` | 根布局，包含导航和播放器 |
| `Header` | `components/layout/header.tsx` | 顶部导航栏 |
| `Footer` | `components/layout/footer.tsx` | 页脚（可选） |

#### 3.1.2 音频相关组件

| 组件名 | 路径 | 功能描述 |
|--------|------|---------|
| `AudioCard` | `components/audio/audio-card.tsx` | 音频卡片（展示基本信息） |
| `AudioPlayer` | `components/audio/audio-player.tsx` | 全局音频播放器 |
| `AudioWaveform` | `components/audio/audio-waveform.tsx` | 波形可视化播放器 |
| `AudioPreview` | `components/audio/audio-preview.tsx` | 音频预览（内嵌播放器） |
| `AudioMeta` | `components/audio/audio-meta.tsx` | 音频元信息展示 |

#### 3.1.3 生成相关组件

| 组件名 | 路径 | 功能描述 |
|--------|------|---------|
| `GenerateForm` | `components/generate/generate-form.tsx` | 音频生成表单 |
| `AudioTypeSelector` | `components/generate/audio-type-selector.tsx` | 音频类型选择器 |
| `StyleSelector` | `components/generate/style-selector.tsx` | 风格选择器 |
| `DurationSlider` | `components/generate/duration-slider.tsx` | 时长滑块 |
| `FormatSelector` | `components/generate/format-selector.tsx` | 格式选择器 |
| `GenerateProgress` | `components/generate/generate-progress.tsx` | 生成进度展示 |

#### 3.1.4 列表相关组件

| 组件名 | 路径 | 功能描述 |
|--------|------|---------|
| `AudioList` | `components/list/audio-list.tsx` | 音频列表容器 |
| `AudioListItem` | `components/list/audio-list-item.tsx` | 列表项 |
| `AudioFilters` | `components/list/audio-filters.tsx` | 筛选面板 |
| `Pagination` | `components/list/pagination.tsx` | 分页组件 |
| `EmptyState` | `components/list/empty-state.tsx` | 空状态展示 |

#### 3.1.5 业务组件

| 组件名 | 路径 | 功能描述 |
|--------|------|---------|
| `SaveModal` | `components/business/save-modal.tsx` | 保存音频弹窗 |
| `DeleteConfirm` | `components/business/delete-confirm.tsx` | 删除确认弹窗 |
| `QuickGenerate` | `components/business/quick-generate.tsx` | 首页快速生成组件 |

### 3.2 核心组件详细设计

#### 3.2.1 AudioCard 组件

```tsx
// 组件Props接口
interface AudioCardProps {
  audio: AudioItem;
  onPlay: (audio: AudioItem) => void;
  onSave?: (audio: AudioItem) => void;      // 仅临时音频显示
  onDelete?: (audio: AudioItem) => void;     // 仅临时音频显示
  onDownload?: (audio: AudioItem) => void;
  showActions?: boolean;
}

// AudioItem 数据结构
interface AudioItem {
  id?: number;                              // 正式记录ID
  temp_id?: number;                         // 临时记录ID
  audio_name?: string;                      // 音频名称（正式表）
  file_name: string;                        // 文件名
  file_format: string;                      // 文件格式
  audio_url: string;                        // 音频URL
  audio_duration: number;                   // 时长（秒）
  file_size: number;                        // 大小（字节）
  style_type: string;                       // 风格类型
  description?: string;                     // 描述
  created_at: string;                       // 创建时间
  updated_at?: string;                      // 更新时间
}
```

#### 3.2.2 GenerateForm 组件

```tsx
// 组件Props接口
interface GenerateFormProps {
  defaultType?: 'sound_effect' | 'background_music';
  onSuccess?: (result: GenerateResult) => void;
  onError?: (error: ErrorInfo) => void;
}

// GenerateResult 响应结构
interface GenerateResult {
  temp_id: number;
  audio_url: string;
  audio_duration: number;
  file_size: number;
  file_format: string;
  style_type: string;
  description: string;
  file_name: string;
  created_at: string;
}

// 表单数据结构
interface GenerateFormData {
  audio_type: 'sound_effect' | 'background_music';
  style_type: string;
  description: string;
  file_format: string;
  duration: number;
}
```

#### 3.2.3 AudioPlayer 全局播放器

```tsx
// 播放器状态
interface PlayerState {
  currentAudio: AudioItem | null;
  isPlaying: boolean;
  currentTime: number;
  duration: number;
  volume: number;
  isMuted: boolean;
}

// 播放器Actions
interface PlayerActions {
  play: (audio: AudioItem) => void;
  pause: () => void;
  resume: () => void;
  stop: () => void;
  seek: (time: number) => void;
  setVolume: (volume: number) => void;
  toggleMute: () => void;
}
```

---

## 第四部分：API 对接方案

### 4.1 API 客户端设计

#### 4.1.1 统一请求封装

```typescript
// services/api/client.ts
import axios, { AxiosInstance, AxiosResponse } from 'axios';

// 统一请求格式
interface ApiRequest<T = unknown> {
  action: string;
  params?: T;
}

// 统一响应格式
interface ApiResponse<T = unknown> {
  success: boolean;
  data?: T;
  error?: {
    code: string;
    message: string;
    details?: unknown;
  };
}

// API 客户端类
class ApiClient {
  private client: AxiosInstance;
  private baseURL: string;

  constructor(baseURL: string) {
    this.baseURL = baseURL;
    this.client = axios.create({
      baseURL,
      timeout: 300000, // 5分钟，用于音频生成
      headers: {
        'Content-Type': 'application/json',
      },
    });

    // 请求拦截器
    this.client.interceptors.request.use(
      (config) => {
        // 可添加鉴权token等
        return config;
      },
      (error) => Promise.reject(error)
    );

    // 响应拦截器
    this.client.interceptors.response.use(
      (response: AxiosResponse<ApiResponse>) => {
        const { data } = response;
        if (!data.success) {
          return Promise.reject(data.error);
        }
        return response;
      },
      (error) => {
        // 处理网络错误
        return Promise.reject({
          code: 'NETWORK_ERROR',
          message: error.message || '网络请求失败',
        });
      }
    );
  }

  // 通用请求方法
  async request<T>(action: string, params?: unknown): Promise<T> {
    const requestData: ApiRequest = { action, params };
    const response = await this.client.post<ApiResponse<T>>('', requestData);
    return response.data.data as T;
  }
}

export const apiClient = new ApiClient('/api/proxy');
```

#### 4.1.2 API 方法封装

```typescript
// services/api/audio-api.ts
import { apiClient } from './client';

// ============ 请求参数类型 ============

// 生成音频参数
interface GenerateAudioParams {
  audio_type: 'sound_effect' | 'background_music';
  style_type: string;
  description: string;
  file_format: string;
  duration: number;
}

// 查询列表参数（通用）
interface QueryAudioParams {
  page?: number;
  page_size?: number;
  file_name?: string;
  file_format?: string;
  audio_duration_min?: number;
  audio_duration_max?: number;
  file_size_min?: number;
  file_size_max?: number;
  style_type?: string;
  description?: string;
  update_time_min?: string;
}

// 临时音频查询参数（特殊字段）
interface QueryTempAudioParams extends Omit<QueryAudioParams, 'update_time_min'> {
  created_at_min?: string;
}

// 保存音频参数
interface SaveAudioParams {
  temp_id: number;
  audio_name: string;
  audio_type: 'sound_effect' | 'background_music';
  description: string;
}

// ============ 响应数据类型 ============

// 生成音频响应
interface GenerateAudioResponse {
  temp_id: number;
  audio_url: string;
  audio_duration: number;
  file_size: number;
  file_format: string;
  style_type: string;
  description: string;
  file_name: string;
  created_at: string;
}

// 音频项
interface AudioItemResponse {
  id: number;
  audio_name: string;
  file_name: string;
  file_format: string;
  audio_url: string;
  audio_duration: number;
  file_size: number;
  style_type: string;
  description: string;
  created_at: string;
  updated_at: string;
}

// 临时音频项（无audio_name）
interface TempAudioItemResponse {
  id: number;
  file_name: string;
  file_format: string;
  audio_url: string;
  audio_duration: number;
  file_size: number;
  style_type: string;
  description: string;
  created_at: string;
  updated_at: string;
}

// 列表响应
interface QueryListResponse<T> {
  total: number;
  page: number;
  page_size: number;
  list: T[];
}

// 保存音频响应
interface SaveAudioResponse extends AudioItemResponse {}

// ============ API 方法 ============

export const audioApi = {
  // 生成音频
  generateAudio: (params: GenerateAudioParams) =>
    apiClient.request<GenerateAudioResponse>('generate_audio', params),

  // 保存音频
  saveAudio: (params: SaveAudioParams) =>
    apiClient.request<SaveAudioResponse>('save_audio', params),

  // 查询音效列表
  querySoundEffects: (params?: QueryAudioParams) =>
    apiClient.request<QueryListResponse<AudioItemResponse>>(
      'query_sound_effects',
      params
    ),

  // 查询背景音乐列表
  queryBackgroundMusic: (params?: QueryAudioParams) =>
    apiClient.request<QueryListResponse<AudioItemResponse>>(
      'query_background_music',
      params
    ),

  // 查询临时音频列表
  queryTempAudio: (params?: QueryTempAudioParams) =>
    apiClient.request<QueryListResponse<TempAudioItemResponse>>(
      'query_temp_audio',
      params
    ),
};
```

### 4.2 Next.js API 代理

```typescript
// app/api/proxy/route.ts
import { NextRequest, NextResponse } from 'next/server';

// 后端工作流URL（从环境变量获取）
const WORKFLOW_URL = process.env.AUDIO_WORKFLOW_URL || 'https://your-workflow-endpoint';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();

    // 转发请求到后端工作流
    const response = await fetch(WORKFLOW_URL, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(body),
    });

    const data = await response.json();
    return NextResponse.json(data);
  } catch (error) {
    return NextResponse.json(
      {
        success: false,
        error: {
          code: 'PROXY_ERROR',
          message: '代理请求失败',
          details: error instanceof Error ? error.message : String(error),
        },
      },
      { status: 500 }
    );
  }
}
```

### 4.3 API 调用示例

```typescript
// services/audio-service.ts
import { audioApi } from '@/services/api/audio-api';
import type {
  GenerateAudioParams,
  GenerateAudioResponse,
  QueryAudioParams,
  SaveAudioParams,
} from '@/services/api/audio-api';

// 音频服务类
class AudioService {
  // 生成音频
  async generateAudio(params: GenerateAudioParams): Promise<GenerateAudioResponse> {
    return audioApi.generateAudio(params);
  }

  // 保存音频
  async saveAudio(params: SaveAudioParams) {
    return audioApi.saveAudio(params);
  }

  // 获取音效列表
  async getSoundEffects(params?: QueryAudioParams) {
    return audioApi.querySoundEffects(params);
  }

  // 获取背景音乐列表
  async getBackgroundMusic(params?: QueryAudioParams) {
    return audioApi.queryBackgroundMusic(params);
  }

  // 获取临时音频列表
  async getTempAudio(params?: QueryAudioParams) {
    return audioApi.queryTempAudio(params);
  }
}

export const audioService = new AudioService();
```

---

## 第五部分：状态管理设计

### 5.1 Store 清单

| Store 名称 | 文件路径 | 职责 |
|-----------|---------|------|
| `playerStore` | `stores/player-store.ts` | 全局音频播放器状态 |
| `generateStore` | `stores/generate-store.ts` | 音频生成状态管理 |
| `listStore` | `stores/list-store.ts` | 列表筛选状态管理 |
| `uiStore` | `stores/ui-store.ts` | UI状态（弹窗、侧边栏等） |

### 5.2 播放器状态管理

```typescript
// stores/player-store.ts
import { create } from 'zustand';
import type { AudioItem } from '@/types/audio';

interface PlayerState {
  // 状态
  currentAudio: AudioItem | null;
  isPlaying: boolean;
  currentTime: number;
  duration: number;
  volume: number;
  isMuted: boolean;
  playlist: AudioItem[];

  // Actions
  setCurrentAudio: (audio: AudioItem | null) => void;
  setIsPlaying: (playing: boolean) => void;
  setCurrentTime: (time: number) => void;
  setDuration: (duration: number) => void;
  setVolume: (volume: number) => void;
  toggleMute: () => void;
  setPlaylist: (list: AudioItem[]) => void;
  playNext: () => void;
  playPrevious: () => void;
  reset: () => void;
}

export const usePlayerStore = create<PlayerState>((set, get) => ({
  // 初始状态
  currentAudio: null,
  isPlaying: false,
  currentTime: 0,
  duration: 0,
  volume: 0.8,
  isMuted: false,
  playlist: [],

  // Actions
  setCurrentAudio: (audio) => set({ currentAudio: audio }),
  setIsPlaying: (playing) => set({ isPlaying: playing }),
  setCurrentTime: (time) => set({ currentTime: time }),
  setDuration: (duration) => set({ duration }),
  setVolume: (volume) => set({ volume, isMuted: volume === 0 }),
  toggleMute: () => set((state) => ({ isMuted: !state.isMuted })),
  setPlaylist: (list) => set({ playlist: list }),

  playNext: () => {
    const { playlist, currentAudio } = get();
    if (!currentAudio || playlist.length === 0) return;
    const currentIndex = playlist.findIndex(
      (item) => item.id === currentAudio.id || item.temp_id === currentAudio.temp_id
    );
    if (currentIndex < playlist.length - 1) {
      set({ currentAudio: playlist[currentIndex + 1] });
    }
  },

  playPrevious: () => {
    const { playlist, currentAudio } = get();
    if (!currentAudio || playlist.length === 0) return;
    const currentIndex = playlist.findIndex(
      (item) => item.id === currentAudio.id || item.temp_id === currentAudio.temp_id
    );
    if (currentIndex > 0) {
      set({ currentAudio: playlist[currentIndex - 1] });
    }
  },

  reset: () =>
    set({
      currentAudio: null,
      isPlaying: false,
      currentTime: 0,
      duration: 0,
    }),
}));
```

### 5.3 生成状态管理

```typescript
// stores/generate-store.ts
import { create } from 'zustand';

type AudioType = 'sound_effect' | 'background_music';
type GenerationStatus = 'idle' | 'generating' | 'success' | 'error';

interface GenerateState {
  // 表单状态
  audioType: AudioType;
  styleType: string;
  description: string;
  fileFormat: string;
  duration: number;

  // 生成状态
  status: GenerationStatus;
  result: GenerateResult | null;
  error: ErrorInfo | null;

  // Actions
  setAudioType: (type: AudioType) => void;
  setStyleType: (style: string) => void;
  setDescription: (desc: string) => void;
  setFileFormat: (format: string) => void;
  setDuration: (duration: number) => void;
  setStatus: (status: GenerationStatus) => void;
  setResult: (result: GenerateResult | null) => void;
  setError: (error: ErrorInfo | null) => void;
  reset: () => void;
}

interface GenerateResult {
  temp_id: number;
  audio_url: string;
  // ... 其他字段
}

interface ErrorInfo {
  code: string;
  message: string;
  details?: unknown;
}

const initialState = {
  audioType: 'sound_effect' as AudioType,
  styleType: '',
  description: '',
  fileFormat: 'MP3',
  duration: 5,
  status: 'idle' as GenerationStatus,
  result: null,
  error: null,
};

export const useGenerateStore = create<GenerateState>((set) => ({
  ...initialState,

  setAudioType: (type) => set({ audioType: type }),
  setStyleType: (style) => set({ styleType: style }),
  setDescription: (desc) => set({ description: desc }),
  setFileFormat: (format) => set({ fileFormat: format }),
  setDuration: (duration) => set({ duration }),
  setStatus: (status) => set({ status }),
  setResult: (result) => set({ result, status: result ? 'success' : 'idle' }),
  setError: (error) => set({ error, status: 'error' }),
  reset: () => set(initialState),
}));
```

---

## 第六部分：类型定义

### 6.1 核心类型文件

```typescript
// types/audio.ts
// ============ 音频类型枚举 ============
export type AudioType = 'sound_effect' | 'background_music';

// ============ 风格类型 ============
export const SOUND_EFFECT_STYLES = [
  '清脆', '浑厚', '尖锐', '柔和', '空灵',
  '沉重', '急促', '悠扬', '机械', '自然',
] as const;

export const BACKGROUND_MUSIC_STYLES = [
  '悲凉', '喜悦', '轻松', '摇滚', '古典',
  '电子', '爵士', '民俗', '悬疑', '浪漫',
] as const;

export type SoundEffectStyle = typeof SOUND_EFFECT_STYLES[number];
export type BackgroundMusicStyle = typeof BACKGROUND_MUSIC_STYLES[number];

// ============ 音频格式 ============
export const AUDIO_FORMATS = ['MP3', 'WAV', 'AAC', 'FLAC', 'OGG'] as const;
export type AudioFormat = typeof AUDIO_FORMATS[number];

// ============ 音频项 ============
export interface AudioItem {
  id?: number;
  temp_id?: number;
  audio_name?: string;
  file_name: string;
  file_format: AudioFormat;
  audio_url: string;
  audio_duration: number;
  file_size: number;
  style_type: string;
  description?: string;
  created_at: string;
  updated_at?: string;
}

// ============ 列表查询参数 ============
export interface QueryParams {
  page?: number;
  page_size?: number;
  file_name?: string;
  file_format?: AudioFormat;
  audio_duration_min?: number;
  audio_duration_max?: number;
  file_size_min?: number;
  file_size_max?: number;
  style_type?: string;
  description?: string;
  update_time_min?: string;
}

// ============ 列表响应 ============
export interface ListResponse<T> {
  total: number;
  page: number;
  page_size: number;
  list: T[];
}

// ============ 生成参数 ============
export interface GenerateParams {
  audio_type: AudioType;
  style_type: string;
  description: string;
  file_format: AudioFormat;
  duration: number;
}

// ============ 保存参数 ============
export interface SaveParams {
  temp_id: number;
  audio_name: string;
  audio_type: AudioType;
  description: string;
}

// ============ 错误信息 ============
export interface ErrorInfo {
  code: string;
  message: string;
  details?: unknown;
}
```

```typescript
// types/api.ts
// ============ API 请求/响应 ============
export interface ApiRequest<T = unknown> {
  action: string;
  params?: T;
}

export interface ApiResponse<T = unknown> {
  success: boolean;
  data?: T;
  error?: ErrorInfo;
}
```

---

## 第七部分：工具函数库

### 7.1 工具函数清单

| 文件路径 | 函数名 | 功能 |
|---------|-------|------|
| `lib/utils/format.ts` | `formatDuration` | 格式化时长（秒 → mm:ss） |
| `lib/utils/format.ts` | `formatFileSize` | 格式化文件大小（字节 → KB/MB） |
| `lib/utils/format.ts` | `formatDate` | 格式化日期 |
| `lib/utils/format.ts` | `formatRelativeTime` | 格式化相对时间 |
| `lib/utils/audio.ts` | `isAudioLoaded` | 检查音频是否已加载 |
| `lib/utils/audio.ts` | `preloadAudio` | 预加载音频 |
| `lib/utils/download.ts` | `downloadAudio` | 下载音频文件 |

### 7.2 工具函数实现

```typescript
// lib/utils/format.ts
/**
 * 格式化时长（秒 → mm:ss）
 */
export function formatDuration(seconds: number): string {
  const mins = Math.floor(seconds / 60);
  const secs = Math.floor(seconds % 60);
  return `${mins}:${secs.toString().padStart(2, '0')}`;
}

/**
 * 格式化文件大小
 */
export function formatFileSize(bytes: number): string {
  if (bytes === 0) return '0 B';
  const k = 1024;
  const sizes = ['B', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return `${parseFloat((bytes / Math.pow(k, i)).toFixed(1))} ${sizes[i]}`;
}

/**
 * 格式化日期
 */
export function formatDate(dateString: string): string {
  const date = new Date(dateString);
  return date.toLocaleDateString('zh-CN', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  });
}

/**
 * 格式化相对时间
 */
export function formatRelativeTime(dateString: string): string {
  const now = new Date();
  const date = new Date(dateString);
  const diff = now.getTime() - date.getTime();

  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);

  if (days > 0) return `${days}天前`;
  if (hours > 0) return `${hours}小时前`;
  if (minutes > 0) return `${minutes}分钟前`;
  return '刚刚';
}
```

```typescript
// lib/utils/audio.ts
/**
 * 预加载音频
 */
export function preloadAudio(url: string): Promise<void> {
  return new Promise((resolve, reject) => {
    const audio = new Audio();
    audio.addEventListener('canplaythrough', () => resolve(), { once: true });
    audio.addEventListener('error', reject, { once: true });
    audio.src = url;
    audio.load();
  });
}

/**
 * 创建音频对象
 */
export function createAudio(url: string): HTMLAudioElement {
  const audio = new Audio();
  audio.crossOrigin = 'anonymous';
  audio.src = url;
  return audio;
}
```

```typescript
// lib/utils/download.ts
/**
 * 下载音频文件
 */
export async function downloadAudio(
  url: string,
  filename: string
): Promise<void> {
  try {
    const response = await fetch(url);
    const blob = await response.blob();
    const blobUrl = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = blobUrl;
    link.download = filename;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    window.URL.revokeObjectURL(blobUrl);
  } catch (error) {
    console.error('Download failed:', error);
    throw error;
  }
}
```

---

## 第八部分：页面实现方案

### 8.1 首页 (/)

**功能**：快速生成音频入口 + 最近生成记录

**布局**：
```
┌─────────────────────────────────────────┐
│  Hero Section                           │
│  ┌─────────────────────────────────────┐│
│  │  🎵 音频素材生成管理系统             ││
│  │  一键生成无版权音效与背景音乐          ││
│  │                                      ││
│  │  [音效生成]  [背景音乐生成]          ││
│  └─────────────────────────────────────┘│
├─────────────────────────────────────────┤
│  Quick Generate Section                 │
│  ┌─────────────────────────────────────┐│
│  │  快速生成                            ││
│  │  ┌────────────────────────────────┐ ││
│  │  │ 描述你想要的声音...            │ ││
│  │  └────────────────────────────────┘ ││
│  │  [音频类型 ▼] [风格 ▼] [格式 ▼]     ││
│  │           [开始生成]                ││
│  └─────────────────────────────────────┘│
├─────────────────────────────────────────┤
│  Recent Audio Section                   │
│  ┌─────────────────────────────────────┐│
│  │  最近生成                           ││
│  │  [AudioCard] [AudioCard] [AudioCard] ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**实现要点**：
- 加载时获取最近生成的临时音频（前5条）
- 快速生成表单简化版，点击"开始生成"后跳转 `/generate` 并填充表单

### 8.2 音效库 (/sound-effects)

**功能**：浏览和管理音效

**布局**：
```
┌─────────────────────────────────────────┐
│  Header                                 │
│  🎧 音效库  [共 128 个音效]              │
├─────────────────────────────────────────┤
│  Filters                                │
│  ┌─────────────────────────────────────┐│
│  │  搜索: [________] 格式: [▼] 风格: [▼] ││
│  │  时长: [3秒] - [10秒]  大小: [▼]     ││
│  │                        [重置] [搜索] ││
│  └─────────────────────────────────────┘│
├─────────────────────────────────────────┤
│  Audio List                             │
│  ┌─────────────────────────────────────┐│
│  │ [播放][下载][收藏] 玻璃破碎声        ││
│  │ [波形图] 3.5s | MP3 | 清脆 | 120KB   ││
│  ├─────────────────────────────────────┤│
│  │ [播放][下载][收藏] 金属碰撞          ││
│  │ [波形图] 2.1s | WAV | 尖锐 | 250KB   ││
│  └─────────────────────────────────────┘│
├─────────────────────────────────────────┤
│  Pagination                             │
│  [< 1 2 3 ... 10 >]                     │
└─────────────────────────────────────────┘
```

**实现要点**：
- 支持分页、筛选、重置
- 点击播放时更新全局播放器
- 无筛选条件时显示空状态

### 8.3 生成页面 (/generate)

**功能**：完整音频生成表单

**布局**：
```
┌─────────────────────────────────────────┐
│  音频生成                               │
├─────────────────────────────────────────┤
│  Step Indicator                          │
│  [1. 选择类型] → [2. 填写描述] → [3. 生成]│
├─────────────────────────────────────────┤
│  Generate Form                          │
│  ┌─────────────────────────────────────┐│
│  │  音频类型                            ││
│  │  ○ 音效  ○ 背景音乐                  ││
│  │                                      ││
│  │  风格类型                            ││
│  │  [清脆][浑厚][尖锐][柔和][空灵]       ││
│  │  [沉重][急促][悠扬][机械][自然]       ││
│  │                                      ││
│  │  音频描述 *                          ││
│  │  ┌────────────────────────────────┐ ││
│  │  │ 请详细描述你想要的声音效果...   │ ││
│  │  └────────────────────────────────┘ ││
│  │                                      ││
│  │  文件格式                            ││
│  │  [MP3] [WAV] [AAC] [FLAC] [OGG]      ││
│  │                                      ││
│  │  时长 (秒)                           ││
│  │  ●────────────● 5.0s                ││
│  │  音效: 1-30s  |  背景音乐: 10-300s    ││
│  │                                      ││
│  │           [取消]  [开始生成]         ││
│  └─────────────────────────────────────┘│
├─────────────────────────────────────────┤
│  Generate Result (生成后显示)           │
│  ┌─────────────────────────────────────┐│
│  │  ✓ 生成成功！                        ││
│  │  [AudioPlayer]                       ││
│  │  时长: 5.2s | 格式: MP3 | 大小: 120KB ││
│  │                                      ││
│  │  音频名称: [________________]        ││
│  │                                      ││
│  │  [取消保存]  [保存到素材库]          ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**实现要点**：
- 使用 React Hook Form + Zod 进行表单验证
- 根据选择的音频类型动态切换风格选项
- 生成过程显示加载状态
- 生成成功后显示预览和保存表单

---

## 第九部分：样式与主题设计

### 9.1 主题配置

```css
/* globals.css */
@import "tailwindcss";

/* 主题色定义 */
:root {
  /* 音频相关色值 */
  --audio-primary: hsl(210 100% 50%);
  --audio-secondary: hsl(180 70% 45%);
  --audio-accent: hsl(340 80% 55%);

  /* 波形图颜色 */
  --waveform-primary: hsl(210 100% 50%);
  --waveform-progress: hsl(210 100% 60%);
  --waveform-cursor: hsl(0 100% 50%);
}

.dark {
  --audio-primary: hsl(210 80% 60%);
  --audio-secondary: hsl(180 60% 50%);
  --waveform-primary: hsl(210 80% 60%);
}
```

### 9.2 音频卡片样式

```tsx
// components/audio/audio-card.module.css
.audioCard {
  @apply relative overflow-hidden rounded-lg border bg-card text-card-foreground 
         shadow-sm transition-all duration-200;
}

.audioCard:hover {
  @apply shadow-md border-primary/20;
}

.audioCardWaveform {
  @apply h-16 bg-muted/50 rounded-md;
}

.audioCardMeta {
  @apply flex items-center gap-2 text-xs text-muted-foreground;
}

.audioCardTag {
  @apply inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium;
}

.audioCardTag清脆 {
  @apply bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-200;
}

/* 风格标签颜色映射 */
.audioCardTag浑厚 { @apply bg-purple-100 text-purple-800; }
.audioCardTag尖锐 { @apply bg-red-100 text-red-800; }
.audioCardTag柔和 { @apply bg-green-100 text-green-800; }
/* ... 其他风格 */
```

---

## 第十部分：项目结构

```
audio-materials-frontend/
├── .coze/                    # Coze CLI 配置
├── public/                   # 静态资源
│   └── favicon.ico
├── src/
│   ├── app/                  # Next.js App Router
│   │   ├── layout.tsx        # 根布局
│   │   ├── page.tsx          # 首页
│   │   ├── globals.css       # 全局样式
│   │   ├── sound-effects/    # 音效库页面
│   │   │   └── page.tsx
│   │   ├── background-music/ # 背景音乐页面
│   │   │   └── page.tsx
│   │   ├── temp-audio/       # 临时音频页面
│   │   │   └── page.tsx
│   │   ├── generate/         # 生成页面
│   │   │   └── page.tsx
│   │   └── api/
│   │       └── proxy/         # API 代理
│   │           └── route.ts
│   ├── components/           # 组件目录
│   │   ├── ui/               # shadcn/ui 组件
│   │   ├── layout/           # 布局组件
│   │   │   ├── app-layout.tsx
│   │   │   └── header.tsx
│   │   ├── audio/            # 音频相关组件
│   │   │   ├── audio-card.tsx
│   │   │   ├── audio-player.tsx
│   │   │   ├── audio-waveform.tsx
│   │   │   └── audio-preview.tsx
│   │   ├── generate/         # 生成相关组件
│   │   │   ├── generate-form.tsx
│   │   │   ├── style-selector.tsx
│   │   │   └── duration-slider.tsx
│   │   ├── list/             # 列表相关组件
│   │   │   ├── audio-list.tsx
│   │   │   ├── audio-filters.tsx
│   │   │   └── pagination.tsx
│   │   └── business/          # 业务组件
│   │       ├── save-modal.tsx
│   │       └── delete-confirm.tsx
│   ├── hooks/                # 自定义Hooks
│   │   ├── use-audio-player.ts
│   │   ├── use-audio-generate.ts
│   │   └── use-audio-list.ts
│   ├── lib/                  # 工具库
│   │   └── utils/
│   │       ├── format.ts
│   │       ├── audio.ts
│   │       └── download.ts
│   ├── services/             # API 服务
│   │   └── api/
│   │       ├── client.ts
│   │       └── audio-api.ts
│   ├── stores/               # 状态管理
│   │   ├── player-store.ts
│   │   ├── generate-store.ts
│   │   ├── list-store.ts
│   │   └── ui-store.ts
│   ├── types/                # 类型定义
│   │   ├── audio.ts
│   │   └── api.ts
│   └── constants/           # 常量定义
│       └── audio-constants.ts
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.js
├── .env.local
└── README.md
```

---

## 第十一部分：环境变量

```bash
# .env.local

# 后端工作流 URL（前端代理用）
NEXT_PUBLIC_WORKFLOW_URL=https://your-workflow-endpoint

# API 代理路径（默认 /api/proxy）
NEXT_PUBLIC_API_PROXY_PATH=/api/proxy
```

---

## 第十二部分：实现步骤计划

### Phase 1: 基础搭建 (Day 1)

- [ ] 初始化 Next.js 项目
- [ ] 配置 Tailwind CSS 和 shadcn/ui
- [ ] 配置主题色和全局样式
- [ ] 创建项目目录结构
- [ ] 实现类型定义文件

### Phase 2: 核心组件 (Day 2-3)

- [ ] 实现 API 客户端封装
- [ ] 实现状态管理 Store
- [ ] 实现布局组件（Header, AppLayout）
- [ ] 实现 AudioCard 组件
- [ ] 实现 AudioPlayer 全局播放器

### Phase 3: 功能页面 (Day 4-5)

- [ ] 实现首页（快速生成入口）
- [ ] 实现音效库页面
- [ ] 实现背景音乐页面
- [ ] 实现临时音频页面
- [ ] 实现生成页面

### Phase 4: 业务组件 (Day 6)

- [ ] 实现筛选组件
- [ ] 实现分页组件
- [ ] 实现保存弹窗
- [ ] 实现删除确认弹窗

### Phase 5: 优化与测试 (Day 7)

- [ ] 响应式布局适配
- [ ] 加载状态优化
- [ ] 错误处理完善
- [ ] 单元测试编写

---

## 第十三部分：注意事项

### 13.1 音频播放注意

1. **跨域问题**：音频 URL 需支持 CORS，建议后端设置 `Access-Control-Allow-Origin`
2. **格式兼容**：不同浏览器支持的音频格式不同，建议后端提供多种格式
3. **加载状态**：大文件需显示加载进度，避免用户等待焦虑

### 13.2 API 调用注意

1. **超时处理**：音频生成接口可能耗时较长，需设置合理的超时时间
2. **错误重试**：网络波动时可考虑重试机制
3. **取消请求**：用户取消时应能中止正在进行的请求

### 13.3 性能优化

1. **音频预加载**：列表页可预加载前几条音频
2. **虚拟列表**：数据量大时可使用虚拟滚动
3. **音频缓存**：使用 AudioContext 缓存已解码的音频数据

---

## 附录：API 接口对照表

| 前端动作 | API Action | 组件/页面 |
|---------|------------|----------|
| 生成音频 | `generate_audio` | GenerateForm |
| 保存音频 | `save_audio` | SaveModal |
| 获取音效列表 | `query_sound_effects` | AudioList (sound-effects) |
| 获取背景音乐列表 | `query_background_music` | AudioList (background-music) |
| 获取临时音频列表 | `query_temp_audio` | AudioList (temp-audio) |

---

*文档版本：v1.0*
*最后更新：2024-01-XX*
